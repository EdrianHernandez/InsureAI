/**
 * Constant object representing the types of insurance available.
 * Replaces the TypeScript enum.
 */
export const InsuranceType = Object.freeze({
  AUTO: 'Auto',
  HOME: 'Home',
  LIFE: 'Life',
});

// Interfaces removed for plain JavaScript conversion.
